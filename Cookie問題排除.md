# Cookie 問題排除指南

如果書籤顯示「找不到 Session Cookie」，請按照以下步驟檢查：

## 🔍 步驟 1: 檢查 Cookie 是否存在

### 方法一：使用檢查工具

1. 在 Suno 歌曲頁面打開 `檢查Cookie.html`
2. 點擊「檢查 Cookie」按鈕
3. 查看找到的 Cookie

### 方法二：手動檢查

1. 按 `F12` 開啟開發者工具
2. 切換到「Application」標籤
3. 找到「Cookies」>「https://suno.com」
4. 查看是否有 `_session` 或類似的 Cookie

## 🔧 步驟 2: 常見問題

### 問題 1: Cookie 名稱不同

Suno 可能使用不同的 Cookie 名稱，例如：
- `_session`
- `__session`
- `_session_U9tc...`
- `_session_Jnx...`

**解決方法：**
- 查看檢查工具顯示的所有 Cookie
- 找到名稱包含 "session" 的 Cookie
- 如果找到，告訴開發者 Cookie 名稱

### 問題 2: Cookie 在不同的域名

Cookie 可能儲存在：
- `suno.com`
- `.suno.com`
- `suno.ai`

**解決方法：**
- 確認你在正確的域名下使用書籤
- 在 `suno.com` 域名下使用

### 問題 3: Cookie 被瀏覽器阻擋

**解決方法：**
1. 檢查瀏覽器設定
2. 確認允許 Cookie
3. 關閉隱私模式
4. 清除 Cookie 後重新登入

### 問題 4: 需要重新登入

**解決方法：**
1. 登出 Suno
2. 清除瀏覽器快取
3. 重新登入 Suno
4. 再次嘗試使用書籤

## 🛠️ 步驟 3: 除錯工具

### 在 Console 中執行

在 Suno 頁面按 `F12`，切換到「Console」標籤，貼上以下程式碼：

```javascript
// 檢查所有 Cookie
console.log('所有 Cookie:', document.cookie);
console.log('Cookie 數量:', document.cookie.split(';').length);

// 檢查特定 Cookie
const checkCookie = (name) => {
    const value = `; ${document.cookie}`;
    const parts = value.split(`; ${name}=`);
    if (parts.length === 2) {
        const cookieValue = parts.pop().split(';').shift();
        console.log(`✅ 找到 ${name}:`, cookieValue.substring(0, 50) + '...');
        return cookieValue;
    } else {
        console.log(`❌ 找不到 ${name}`);
        return null;
    }
};

checkCookie('_session');
checkCookie('__session');
checkCookie('session');
```

## 📝 步驟 4: 回報問題

如果以上方法都無法解決，請提供以下資訊：

1. **Cookie 檢查結果**
   - 使用 `檢查Cookie.html` 的結果
   - 或 Console 的輸出

2. **瀏覽器資訊**
   - 瀏覽器名稱和版本
   - 作業系統

3. **錯誤訊息**
   - 書籤顯示的完整錯誤訊息
   - Console 中的錯誤（如果有）

## 💡 臨時解決方案

如果書籤無法自動取得 Cookie，可以使用：

1. **HTML 版本工具** (`suno-subtitle-downloader.html`)
   - 手動輸入 Session Cookie
   - 從開發者工具複製 Cookie 值

2. **Python 版本**
   - 使用命令行或 GUI 版本
   - 手動輸入 Cookie

## ✅ 確認清單

- [ ] 已登入 Suno 帳號
- [ ] 在 `suno.com` 域名下
- [ ] 在歌曲頁面（網址包含 `/song/`）
- [ ] 瀏覽器允許 Cookie
- [ ] 已檢查開發者工具中的 Cookie
- [ ] 已嘗試重新登入
